[package]
name = "cipher-rs"
version = "0.1.0"
edition = "2024"

[dependencies]
phf = { version = "0.13.1", features = ["macros"] }
itertools = "0.14.0"
eta = "0.2.2"


[profile.release]
codegen-units = 1
lto = false
